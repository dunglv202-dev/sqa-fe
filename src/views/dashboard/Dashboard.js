import { Box } from '@mui/material';
import PageContainer from 'src/components/container/PageContainer';

const Dashboard = () => {
  return (
    <PageContainer title="Quản lý vay lãi & gửi tiết kiệm">
      <Box></Box>
    </PageContainer>
  );
};

export default Dashboard;
